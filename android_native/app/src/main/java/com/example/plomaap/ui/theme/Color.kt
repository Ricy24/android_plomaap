package com.example.plomaap.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant Accents
val SapphireBlue = Color(0xFF0F52BA)
val SapphireBlueDark = Color(0xFF0A3D8F)
val SapphireBlueLight = Color(0xFF3A7BD5)
val JadeGreen = Color(0xFF00C9A7)
val JadeGreenDark = Color(0xFF00A88A)
val JadeGreenLight = Color(0xFF33D4B8)
val ElectricCyan = Color(0xFF00E5FF)

// Light Theme Fundamentals
val BackgroundLight = Color(0xFFF7F9FC) // Very light gray-blue
val SurfaceLight = Color(0xFFFFFFFF) // Pure white cards
val SurfaceElevatedLight = Color(0xFFF0F4F8) // Slightly elevated

// Semantic Aliases
val PrimaryBlue = SapphireBlue
val SurfaceWhite = SurfaceLight
val ForestGreen = JadeGreen

// Text
val TextPrimary = Color(0xFF1C2B41) // Dark charcoal blue
val TextSecondary = Color(0xFF5B6C83) // Slate gray
// Kept dark enough to be legible on white cards and in small supporting labels.
val TextTertiary = Color(0xFF65758B) // Accessible slate
val TextOnPrimary = Color(0xFFFFFFFF) // White on buttons

// Status Colors
val StatusScheduled = Color(0xFF42A5F5)
val StatusCompleted = Color(0xFF66BB6A)
val StatusCancelled = Color(0xFFEF5350)
val StatusInProgress = Color(0xFFFFA726)

// Gradients
val GradientPrimaryStart = SapphireBlue
val GradientPrimaryEnd = ElectricCyan
val GradientAccentStart = JadeGreen
val GradientAccentEnd = SapphireBlueLight

// UI Elements
val DividerColor = Color(0xFFE2E8F0)
val ShimmerBase = Color(0xFFE2E8F0)
val ShimmerHighlight = Color(0xFFF7F9FC)
val ErrorRed = Color(0xFFFF5252)
val StarGold = Color(0xFFFFD54F)
val AmberWarning = Color(0xFFFFB300)
val GoogleRed = Color(0xFFDB4437)
val GlassWhite = Color(0x66FFFFFF) // Frosty glass
val GlassBorder = Color(0x33000000) // Subtle border
val ShadowColor = Color(0xFF0F52BA) // Tinted blue shadow
